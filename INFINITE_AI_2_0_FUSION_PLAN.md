# Infinite AI 2.0 融合实施方案

> 文档状态：核心产品决策已确认，作为分阶段实施基线
> 适用范围：Infinite AI 网页端、桌面端、CLI、统一网关与管理后台
> 编写原则：先明确边界、冲突、数据模型与验收标准；经确认后分阶段实施
> 重要说明：本文中的“目标状态”不代表当前代码已经完成。所有上线能力必须以测试矩阵和验收记录为准。

## 当前实施进度（2026-08-03）

已完成并经过本地 PostgreSQL 集成测试的基础闭环：

- PostgreSQL 迁移、受控 SQLite 导入预检/离线导入、平台模型、产品发布、路由池、第三方 OpenAI 兼容上游、账号目录同步、用户邀请、用户会话、外部 API Key、三类独立钱包和不可变账本。
- 新公共网关开关 `LITE_PLATFORM_GATEWAY_ENABLED`：开启后 `/v1/models`、`/v1/responses`、`/v1/chat/completions` 只接受 PostgreSQL API Key；模型别名按 Key Scope 与产品发布解析，再映射到私有上游模型 ID。
- 同协议 OpenAI 兼容路径保持工具 JSON、图片输入等请求字段原样，只替换顶层 `model`；SSE/JSON 响应原样透传，可信 usage 结算，缺失 usage 明确标记为估算。
- Key 停用/删除会取消已经在途的 PostgreSQL 网关请求；下一请求也会立即被拒绝。邀请撤销/删除由数据库状态机即时阻断领取。
- 网页认证在该开关开启时切换到 PostgreSQL 用户、独立 Cookie 和 CSRF；注册默认领取 Free 的 Chat 与 Agent 两套独立额度桶，用户模型列表只返回后台发布的平台模型；`/api/portal/chat/responses` 使用 Chat 钱包和同协议上游路由。网页退出会取消在途 Chat 请求。
- Chat 完整会话管理已接入 PostgreSQL：支持会话列表、新建、读取、改名、归档/删除、按会话继续发送、用户/助手消息落库、Portal 响应中的公开模型别名净化，以及网页端历史侧栏和安全 Markdown 渲染。
- Agent 项目 API、桌面端 PostgreSQL 设备身份迁移和本地子 Key 已接入统一域：桌面端使用服务端签发设备会话和本机子 Key，设备/会话撤销会同步阻断在途 Agent 请求和本地子 Key。
- PostgreSQL 平台全量加密导出/导入已接入现有备份入口：平台开关开启时导出 `FGLTPG01` 平台负载，恢复前会取消在途平台请求、清空瞬态会话/Nonce/本地子 Key，并将平台凭据、API Key、设备 MAC、管理员 TOTP 等密文重加密到目标环境 Master Key。
- 第三方兼容协议扩展完成：公共 API 与桌面 Agent 网关支持管理员配置的 `openai_compatible`、`anthropic_compatible`、`gemini_compatible` 上游，分别代理 `/v1/responses`、`/v1/chat/completions`、`/v1/messages`、`/v1beta/models/{model}:generateContent` 和 `:streamGenerateContent`；不同协议使用各自鉴权方式、私有模型 ID 映射和 usage 结算解析。
- 统一平台后台已接入 PostgreSQL 的真实调用仪表盘、模型排行、调用记录、钱包余额与平台审计；可在后台创建/停用/删除兼容上游和账号、同步模型、配置路由、管理新 API Key、邀请、用户状态、套餐版本以及向独立产品钱包手工充值 Token。管理员动作同时进入 PostgreSQL 平台审计。
- 额度周期补齐：Chat 与 Agent 的五小时窗口到期后才会创建下一窗口；Free 订阅到期后自动续为当前 Free 版本。External API 保持独立的零初始额度钱包，只能由后续支付或管理员充值供给，绝不从 Chat/Agent 额度扣减。
- 支付控制面骨架已接入 PostgreSQL：后台可查看支付商户配置和订单、加密保存待验证商户配置、显式保持停用；真实下单、回调验签、退款和对账未通过商户验收前不会在用户端启用。

仍在实现、不能视为已经可用的部分：支付商户对接，以及 ChatGPT、Claude、Gemini、Antigravity、xAI、Kiro 的各自官方 OAuth 生命周期。它们在通过官方应用凭证、独立协议和真实账号验收前必须保持“未配置/不可用”。

## 1. 项目目标

Infinite AI 2.0 将当前分散的网页聊天、Agent 客户端、模型网关、账号池、邀请、API Key、设备授权、计费、安全与运维能力融合为一个可部署、可审计、可扩展的完整产品。

核心目标如下：

1. 一个权威后端：所有用户、设备、项目、上游凭证、模型、额度、账单、会话、调用记录和安全策略均由同一后端管理。
2. 两种用户产品：
   - **Chat**：浏览器中的网页聊天产品。
   - **Agent**：桌面端、CLI、编辑器插件及工具执行环境。
3. 一套统一模型路由能力：Chat 和 Agent 共用后台创建的平台模型、上游账号池和计费基础设施，但两种产品的套餐额度、余额账户和用量统计完全分账；Agent 同时具备 Chat 与工具能力，网页 Chat 不调用本地 Agent 工具。
4. 集中配置：普通用户不能添加供应商、Base URL、上游 API Key、OAuth 账号或系统提示词，所有模型与安全策略由平台统一管理。
5. 平台计费：用户只能购买额度、使用兑换/邀请权益，或由管理员手工充值，不提供用户自带上游密钥模式。
6. 多上游：支持 ChatGPT、Claude、Gemini、Antigravity、xAI、Kiro 的合法 OAuth/凭证接入，以及第三方兼容 API 接入。
7. 账号池隔离：不同用户、Key、项目和会话不能串台；故障转移不能破坏会话粘滞和工具调用上下文。
8. 全链路可观测：每一次 API、Chat、Agent、工具、生图、文件和搜索调用都能在管理后台形成真实记录，并关联用户、Key、设备、项目、模型、上游账号、Token、费用、延迟和错误。
9. 小规格可运行：默认部署形态应能在 1 核 CPU、2 GB 内存服务器上稳定运行。
10. 可迁移与可恢复：支持完整加密导出、在新环境导入、版本迁移、备份验证和回滚。

## 2. 非目标与明确限制

本阶段不做以下事情：

- 不同时保留两套相互竞争的用户、计费、模型或会话数据库。
- 不让客户端直接持有上游 OAuth Refresh Token 或第三方供应商密钥。
- 不允许普通用户绕开平台选择任意 Base URL、代理地址或系统提示词。
- 不把同一请求广播到多个上游，不做不可控的“竞速请求”。
- 不声称跨协议转换能够保证供应商私有能力完全无损；只有能力矩阵标记为“原生”或“已适配并通过测试”的能力才对外开放。
- 不用 MAC 地址作为唯一安全凭证。Agent 强制登记 MAC 作为辅助绑定和风险信号，但密码学设备公钥仍是硬身份，因为公网服务无法从普通 HTTP 请求独立读取真实网卡 MAC。
- 不提交任何 OAuth 客户端秘密、测试账号、测试 Token、生产密钥、管理员密码或真实用户数据。
- 不通过非官方、不可维护或违反上游服务条款的方式实现 OAuth。
- 不保留外部参考实现的代码、资源、名称、注释、提交历史、目录结构或许可证文本。

## 3. 术语

| 术语 | 定义 |
|---|---|
| 用户 | 使用网页、桌面端、CLI 或插件的最终用户 |
| 管理员 | 可管理上游、模型、价格、余额、用户、安全和系统设置的后台操作者 |
| Agent | 桌面端、CLI、插件及其工具调用能力 |
| Chat | 网页聊天及不执行本地工具的普通对话能力 |
| 项目 | Agent 可访问的工作区边界，包含路径授权、策略、成员和用量归属，不包含用户自定义上游配置 |
| 上游连接器 | 负责某类供应商认证、模型发现、请求、流式响应、错误映射和额度同步的适配模块 |
| 上游账号 | 一份 OAuth 或其他平台托管凭证，对应一个供应商身份 |
| 第三方 API | 管理员录入的兼容 API 服务及其平台密钥 |
| 路由池 | 一组能力兼容、可被调度的上游账号或 API 端点 |
| 会话粘滞 | 同一 Key、用户、项目和会话在有效期内固定使用同一上游账号 |
| 钱包账户 | 用户在指定产品范围内可消费余额的汇总视图；Chat 与 Agent 分别建账 |
| 账本 | 每笔充值、消费、退款、冻结和调整的不可变流水 |
| 设备凭证 | 桌面端生成的非对称密钥身份及服务端签发会话，不等同于 MAC 地址 |
| 本地子 Key | 由已登录桌面端临时签发、仅绑定本机代理的受限密钥 |

## 4. 总体架构

### 4.1 推荐部署形态

目标架构采用**一个总的 Go 后端**、一套 PostgreSQL 数据库、一个受权限保护的数据目录和同仓库前端资源。这个 Go 后端同时承载认证、用户、管理、Chat API、Agent API、兼容网关、OAuth、上游路由、计费、邀请、安全与运维能力，不再常驻第二套 BFF 或业务后端。桌面端继续使用现有 Rust/Tauri 与 React 技术栈，但所有账号、模型和计费策略来自统一 Go 后端。

当前 Go Lite 后端仍使用 SQLite；它是迁移输入，不是目标数据库。实施时先建立 PostgreSQL Schema 和迁移工具，在数据校验通过后一次性切换权威数据库，不能让 SQLite 与 PostgreSQL 长期双写或分别成为不同功能的权威。

```text
浏览器 Chat ───────┐
桌面 Agent ───────┤
CLI / 编辑器插件 ─┼──> Infinite AI 统一网关 ──> 路由/计费/审计 ──> 上游连接器
外部 API 客户端 ──┘             │                         ├─ OAuth 账号池
                                │                         ├─ 第三方 API 池
管理后台 ────────────────────────┤                         └─ 代理池
邀请/指南 ──────────────────────┘
                                │
                         PostgreSQL + 加密密钥库
```

### 4.2 唯一权威原则

- Go 后端是用户、权限、余额、上游、模型目录、调用记录和策略的唯一权威。
- Go 后端可在一个进程内监听 API、管理、邀请、指南和 Chat 端口；端口分离不等于拆分为多个业务后端。
- 网页端不得维护第二套用户或余额数据。
- 桌面端只保存最小化的设备私钥、短期访问令牌、刷新凭证和本地偏好。
- 旧系统中重复的数据库、消息队列、对象存储和缓存服务不作为默认运行依赖。
- 如果未来业务量需要拆分，优先从日志归档、对象存储和异步任务开始，不能先拆认证与账本。

### 4.3 端口规划

| 默认端口 | 用途 | 公网策略 |
|---:|---|---|
| 18080 | OpenAI/Anthropic/Gemini 兼容 API、Agent API | 可选公网；必须鉴权 |
| 18081 | Infinite AI 管理后台 | 建议仅内网、VPN 或强访问控制 |
| 18082 | 一次性邀请领取页 | 可公网；独立限速与封禁 |
| 18083 | 配置指南页 | 可公网；Key/海报凭证鉴权 |
| 18084 | Infinite AI Chat 用户网页 | 可公网；Cookie 会话鉴权 |

生产环境由 Caddy 或 Nginx 终止 TLS 并按域名反代。端口分离保留，但可以映射为不同子域名。管理端、邀请端和用户端 Cookie 必须使用不同名称和 Path/Domain 范围，配置指南的清理策略不得影响管理端 Cookie。

## 5. 推荐根目录结构

```text
/
├── cmd/                         # Go 程序入口
├── internal/
│   ├── auth/                    # 管理员、用户、设备、API Key 鉴权
│   ├── upstream/                # 上游连接器与注册表
│   │   ├── chatgpt/
│   │   ├── claude/
│   │   ├── gemini/
│   │   ├── antigravity/
│   │   ├── xai/
│   │   ├── kiro/
│   │   └── compatible/
│   ├── routing/                 # 池调度、粘滞、熔断、重试
│   ├── protocol/                # OpenAI/Anthropic/Gemini 协议适配
│   ├── billing/                 # 钱包、账本、价格快照、退款
│   ├── projects/                # 项目、成员和 Agent 策略
│   ├── tools/                   # 工具目录、授权、审计
│   ├── security/                # 限速、封禁、SSRF、CSRF、风控
│   ├── observability/           # 指标、日志、调用追踪
│   ├── store/                   # PostgreSQL 仓储、事务和迁移
│   └── web/                     # 嵌入式管理/邀请/指南静态资源
├── web/                         # Chat 网页端源码
├── desktop/                     # Agent 桌面端源码
├── cli/                         # CLI 源码
├── deploy/                      # systemd、Docker、反向代理模板
├── scripts/                     # 安装、升级、备份、验收脚本
├── migrations/                  # 版本化数据库迁移
├── docs/                        # 用户和运维文档
├── tests/                       # 集成、安全、协议与性能测试
└── releases/                    # 构建产物说明，不提交临时缓存
```

迁移时可逐步形成以上结构，不要求一次性移动所有文件。任何目录重构必须单独提交并保持功能不变，避免与业务改造混在同一提交中。

## 6. 产品边界：Agent 与 Chat

### 6.1 Chat

Chat 提供：

- 注册、登录、退出、会话设备管理。
- 新建、重命名、归档和删除聊天。
- 选择管理员允许的模型。
- SSE 流式文本、推理摘要、引用和错误恢复。
- 图片上传、文件上传、生图、网页搜索等已被上游和套餐允许的能力。
- 消息反馈、重试、分支和继续生成。
- 余额、消费明细、套餐和充值入口。
- 可使用网页搜索、生图、文件理解等后台允许的模型原生或服务端能力，但不连接用户本机，不调用 Shell、项目文件写入、浏览器控制等本地 Agent 工具。

### 6.2 Agent

Agent 提供：

- 使用浏览器完成网页登录和设备授权。
- 绑定项目工作区，而不是绑定用户自定义供应商。
- 读取、修改项目文件，执行终端、搜索、Git、MCP/工具等操作。
- 普通 Chat 对话能力。
- 通过平台模型目录选择模型；实际可选范围由用户套餐、项目策略和设备策略的交集决定。
- 本地敏感操作确认、权限沙箱和审计。
- 在桌面端运行期间创建本地子 Key，为本机其他程序提供受限兼容 API。
- Agent 同时具备普通 Chat 能力；即使本次只聊天，请求的产品归属仍为 Agent，并扣除 Agent 套餐额度。

### 6.3 普通用户不可配置项

以下字段仅管理员可配置，普通用户界面不显示编辑入口，服务端也必须拒绝越权写入：

- 上游供应商类型。
- OAuth 凭证、API Key、Client Secret。
- Base URL、代理 URL、TLS 跳过验证。
- 平台系统提示词和隐藏策略提示词。
- 模型成本、销售价格、倍率和免费额度。
- 全局工具白名单、危险命令策略。
- 路由池成员、优先级和故障转移规则。

普通用户仍可配置不影响平台控制面的偏好，例如主题、语言、快捷键、是否自动滚动、项目文件排除规则和本地操作确认等级。

## 7. 项目模型

“只能用项目”定义为：Agent 的所有工具会话必须归属某个项目，项目决定其工作区和权限边界，但不能由用户借项目注入上游配置。

项目至少包含：

- 项目 ID、名称、所有者、成员和状态。
- 工作区指纹与客户端本地路径映射；服务端不保存用户绝对路径作为认证依据。
- 允许模型集合、默认模型和最大上下文策略。
- 允许工具集合及每类工具的确认级别。
- 服务端系统提示词版本 ID；用户不可读取隐藏内容。
- 月度或总额度上限、单次最大费用、并发限制。
- 数据保留策略和审计等级。
- 创建、更新时间和最后活动时间。

普通用户可以在 Agent 中创建、重命名、归档和删除自己的本地项目，也可以在管理员为 Agent 发布的模型范围内选择项目模型。用户不能借项目修改供应商、Base URL、上游 API Key、OAuth 账号、代理、系统提示词或平台工具定义；这些字段即使绕过前端直接请求接口也必须返回 403。

服务端对每个 Agent 请求验证：用户会话有效、设备有效、项目成员关系有效、项目状态有效、模型被允许、工具被允许、钱包可用、请求签名与重放保护通过。

## 8. 身份、会话与权限模型

### 8.1 角色

| 角色 | 权限 |
|---|---|
| 超级管理员 | 系统初始化、管理员、密钥轮换、导入恢复和全部配置 |
| 运营管理员 | 用户、充值、邀请、价格、套餐和用量查询 |
| 上游管理员 | OAuth 账号、第三方 API、代理池、模型同步和健康检查 |
| 安全审计员 | 只读审计、安全事件、封禁管理，可按授权解封 |
| 普通用户 | Chat、Agent、项目、余额和自己的会话设备 |
| 外部 API 使用者 | 仅在 Key Scope 与网络策略允许的接口调用 |

必须采用服务端 RBAC，不以“前端没有按钮”作为权限控制。

### 8.2 管理员认证

- 首次初始化只在数据库中不存在管理员时开放。
- 创建首个管理员后永久关闭初始化接口；重新开放只能由本机命令行执行显式恢复流程。
- 密码使用 Argon2id，参数按 1 核 2G 校准并保留算法升级字段。
- Microsoft Authenticator 采用标准 TOTP，而不是依赖某个厂商在线接口。
- TOTP 秘钥使用主密钥加密，提供一次性恢复码且仅展示一次。
- 登录成功使用 HttpOnly、Secure、SameSite Cookie；状态变更接口验证 CSRF Token 和 Origin。
- 高风险操作要求近期重新验证密码和 TOTP。
- 后台会话可逐个撤销；改密后立即撤销其他全部管理员会话。

### 8.3 用户认证

- 邮箱/用户名加密码是基础能力，后续可增加登录 OAuth，但与上游模型 OAuth 完全分离。
- 注册策略支持 `closed`（关闭）、`invite_only`（邀请制）和 `public`（公开）三种模式，默认值为 `invite_only`。
- `closed` 模式下所有公开注册接口统一拒绝，仅管理员可创建用户；`invite_only` 必须提交未过期、未撤销、未使用且符合角色/额度规则的用户邀请；`public` 才显示和开放普通注册。
- 注册策略由管理员修改，修改动作进入审计并立即生效；前端是否显示注册按钮由服务端公开配置决定，但服务端始终独立执行策略校验。
- 用户注册邀请与“生成外发 API Key”的邀请使用不同类型和 Scope，不能拿其中一种兑换另一种资源。
- 密码哈希、Cookie、CSRF、重放防护与管理员采用独立命名空间。
- 浏览器已有有效 Cookie 时直接进入 Chat，不显示独立登录页。
- 未登录状态保留 Chat 壳层和登录/注册入口，打开半透明认证弹窗。
- 网页可实时撤销桌面设备会话；桌面端下次请求立即失败并退回登录门禁。

### 8.4 设备认证

- 桌面端首次启动在系统安全存储中生成 Ed25519 或 P-256 设备密钥对。
- 浏览器设备码流程绑定用户、设备公钥、发起 IP、批准 IP、过期时间和一次性状态。
- 后续请求以短期 Access Token、设备签名、时间戳、Nonce 和请求体摘要共同鉴权。
- Refresh Token 仅存哈希；轮换使用，一旦复用则撤销整个令牌族。
- 使用 Agent 必须完成设备注册并绑定至少一个稳定物理网卡 MAC；网页 Chat 不采集、不要求也不能通过浏览器读取 MAC。
- Agent 经过用户授权采集物理网卡 MAC 集合，过滤回环、广播、明显虚拟和临时随机网卡；服务端保存 AES-GCM 密文和带服务器密钥的不可逆 HMAC。
- 设备公钥是硬性密码学身份，MAC 与登录时观测到的 IPv4/IPv6 是强制登记的辅助绑定和异常检测信号。请求必须先通过设备签名，不能只凭 MAC 获得权限。
- Agent 每次建立或刷新会话时，在设备签名保护下提交当前 MAC 指纹集合。已登记稳定 MAC 消失或全部改变时，立即暂停 Agent 会话、撤销本地子 Key 并要求重新网页验证，不直接永久封禁用户。
- MAC 可以被修改或伪造，公网服务器也无法独立读取真实 MAC，因此它不能替代设备公钥、密码、Cookie 或 TOTP。

## 9. 上游连接器统一抽象

每个连接器实现统一接口，避免在代理主流程写供应商条件分支：

```go
type Connector interface {
    Kind() ProviderKind
    AuthModes() []AuthMode
    BeginAuth(ctx context.Context, req AuthRequest) (AuthFlow, error)
    CompleteAuth(ctx context.Context, result AuthResult) (Credential, error)
    Refresh(ctx context.Context, credential Credential) (Credential, error)
    DiscoverModels(ctx context.Context, credential Credential) ([]Model, error)
    Probe(ctx context.Context, credential Credential) HealthResult
    Quota(ctx context.Context, credential Credential) (QuotaSnapshot, error)
    Invoke(ctx context.Context, request CanonicalRequest) (CanonicalResponse, error)
    Stream(ctx context.Context, request CanonicalRequest) (EventStream, error)
}
```

连接器还必须声明能力位：文本、视觉输入、生图、文件、网页搜索、函数工具、并行工具、结构化输出、推理强度、SSE、WebSocket、提示缓存和最大上下文。

## 10. OAuth 与凭证生命周期

### 10.1 统一 OAuth 流程

所有上游 OAuth 采用以下状态机：

```text
新建授权 -> 生成 PKCE/State/Nonce -> 展示授权链接
        -> 用户在官方页面授权 -> 回填回调 URL 或自动回调
        -> 校验 State/Nonce/Code Verifier -> 交换 Token
        -> 获取上游身份 -> 加密保存 -> 同步模型/额度 -> 健康
```

必须满足：

- State、Nonce、PKCE Verifier 为高强度随机值，只能使用一次且短期过期。
- 管理端支持“生成链接后手工粘贴 localhost 回调 URL”，用于后台服务器无法接收本机回调的场景。
- 完成授权时严格比对协议、主机、State 和 Code，禁止把任意 URL 当作回调访问，避免 SSRF。
- Access Token、Refresh Token、客户端秘密和账号 Cookie 使用 AES-256-GCM 分字段加密；列表接口永不返回明文。
- 管理员复制敏感值需要近期重新认证、审计和短时展示。
- 刷新任务采用单飞锁，避免并发刷新使 Refresh Token 失效。
- 刷新后原子替换新凭证；失败保留可恢复状态并进入冷却，而不是立即删除账号。
- 删除账号前检查活跃会话和池成员；删除后立即取消关联的进行中请求、清除模型快照和粘滞关系。

### 10.2 ChatGPT

- 支持官方 OAuth 授权链接、PKCE、手工粘贴 localhost 回调。
- 保存上游账号 ID、计划类型、Token 到期时间、模型快照、5 小时/7 天额度窗口和重置时间。
- 支持自动刷新、额度周期同步、模型列表同步和真实探测请求。
- 请求携带的会话类标识必须通过 `平台密钥 + 用户 + 项目 + 会话` 生成不可逆命名空间，不能跨租户复用原始 ID。
- 工具 JSON、SSE 事件和二进制内容在同协议情况下保持字段和顺序；只过滤明确不允许透传的敏感请求头。

### 10.3 Claude

- 连接器预留 OAuth、Token 导入和管理员 API 凭证模式，但只启用官方公开且允许服务器使用的授权方式。
- 支持 Anthropic Messages 原生请求，以及来自 OpenAI Responses/Chat 的能力受限转换。
- 同步模型、上下文、视觉、工具、缓存等能力。
- OAuth 不可用或客户端凭证未配置时，后台明确显示“未配置/上游未开放”，不能显示可点击的假授权按钮。

### 10.4 Gemini

- 上游模型账号授权与用户登录 OAuth 分离。
- 支持 OAuth 与管理员服务凭证/API 凭证，根据官方接口能力选择。
- 支持 Gemini 原生生成接口、流式响应、多模态输入、文件引用和工具调用。
- 记录项目/配额维度，避免不同凭证在云项目层面共享配额却被错误当成独立账号。

### 10.5 Antigravity

- 作为独立连接器类型管理，不与 Gemini 凭证混表。
- 支持官方授权流程可提供的账号登录、Token 刷新、模型发现、额度同步和请求入口。
- 如果其底层模型来自同一云平台，也必须保留独立的凭证来源、请求头、能力快照和错误映射。
- 在正式实现前用真实测试账号确认官方授权端点、Scope、回调约束和自动化许可，并把结论写入连接器验收记录。

### 10.6 xAI

- 同时设计管理员 API Key 与 OAuth 两种认证模式。
- OAuth 仅在官方面向该用途开放授权端点和 Scope 后启用；否则后台只显示 API 模式可用。
- 支持文本、推理、视觉、生图或实时能力时，以真实模型发现与探测结果为准。
- 模型价格和能力必须按版本快照保存，不能只按模糊别名计费。

### 10.7 Kiro

- 独立保存账号身份、Access/Refresh Token、区域或端点信息、额度和套餐状态。
- 支持 Token 自动刷新、健康检查、冷却、模型发现和额度同步。
- 对外提供 Anthropic 或 OpenAI 兼容接口时走显式协议适配层，不把供应商私有字段伪装成通用字段。
- 对账号池中的每个凭证执行独立代理、速率和失败状态管理。

### 10.8 OAuth 账号状态机

| 状态 | 含义 | 是否参与调度 |
|---|---|---:|
| pending | 授权未完成 | 否 |
| healthy | 授权、模型、探测正常 | 是 |
| refreshing | 正在单飞刷新 | 已有请求可选，新请求谨慎 |
| cooldown | 临时限流或连续错误 | 否 |
| exhausted | 当前额度窗口耗尽 | 否，重置后自动恢复 |
| reauth_required | Refresh Token 失效 | 否 |
| disabled | 管理员停用 | 否，立即生效 |
| deleted | 已软删除并等待清理 | 否 |

## 11. 第三方 API 接入

第三方 API 只能由管理员添加，支持以下类型：

- OpenAI Responses 兼容。
- OpenAI Chat Completions 兼容。
- Anthropic Messages 兼容。
- Gemini GenerateContent 兼容。
- 自定义连接器插件只允许编译期或签名扩展包，不允许用户上传任意代码。

每个第三方端点配置：

- 显示名称、协议类型、Base URL、认证方式、加密 API Key。
- 可选组织/项目头，但必须使用固定白名单头名称。
- 模型发现方式：远端模型接口、管理员静态映射或两者合并。
- 能力声明与探测结果；声明和实测冲突时以更保守结果为准。
- 请求超时、首包超时、最大响应体、重试策略和并发上限。
- 独立代理或代理池。
- 成本价格、销售价格、币种和版本生效时间。
- TLS 校验始终默认开启；生产环境禁止“跳过证书验证”。

Base URL 必须经过 SSRF 防护：仅允许 HTTPS（本机受控测试例外）、解析并拒绝环回/链路本地/私网/保留地址、限制重定向、重定向后重新校验、阻止 DNS Rebinding，并支持管理员显式的受审计内网白名单。

## 12. 代理与代理池

### 12.1 代理层级

优先级从高到低：

1. 上游账号绑定的固定代理。
2. 上游账号绑定的代理池。
3. 供应商级默认代理池。
4. 系统全局代理。
5. 直连。

支持 HTTP、HTTPS CONNECT 和 SOCKS5。代理凭证加密保存，前端仅展示掩码。

### 12.2 代理池调度

- 每个代理记录地区、出口 IPv4、出口 IPv6、协议、延迟、最后成功、连续失败和冷却时间。
- OAuth 授权、Token 刷新、额度同步、模型同步和模型调用默认使用同一账号的同一代理出口，避免风控异常。
- 固定代理失败时是否允许切换由账号策略决定；默认不自动跨地区切换。
- 健康检查使用供应商允许的轻量端点，不能用高成本生成请求持续探测。
- 代理失败与账号失败分别统计，防止错误地停用有效账号。
- 管理员可测试代理，但测试结果必须显示真实时间、目标、出口 IP 和错误。

## 13. 模型目录与能力矩阵

### 13.1 三层模型定义

1. **上游模型**：OAuth 账号或第三方 API 真实发现/配置的模型 ID 和能力，只在管理后台可见。例如不同连接器分别提供各自的真实模型 ID。
2. **平台模型**：管理员在 Infinite AI 后台手动创建的稳定模型实体，包含平台模型 ID、显示名称、说明、图标、能力策略和状态。它不要求与任何真实上游模型同名。
3. **产品发布配置**：决定某个平台模型是否在 Chat、Agent 或外部 API 中显示，以及各产品的价格、套餐权限、排序和默认状态。

Chat 与 Agent 前端只读取后台发布的平台模型，不直接展示 OAuth 模型列表，也不直接展示第三方 API 的模型列表。因此前端出现的名称可以完全是平台自定义名称，例如 `infinite-pro`；真实上游可能是其他任意模型 ID。

平台模型可以先创建后绑定路由，但没有任何健康目标时必须显示“暂不可用”并禁止发起请求，不能用假响应掩盖未配置状态。上游模型同步后保存版本化快照，并由探测任务确认关键能力。

### 13.2 多上游指向同一平台模型

一个平台模型可以同时绑定多个 OAuth 和第三方 API 目标：

```text
后台平台模型：infinite-pro
├── OAuth 账号池 A  → 真实模型 ID A
├── OAuth 账号池 B  → 真实模型 ID B
├── 第三方 API 端点 A → 真实模型 ID C
└── 第三方 API 端点 B → 真实模型 ID D
```

每条映射称为 `model_route_target`，至少包含：

- 平台模型 ID。
- 连接器、OAuth 账号池或第三方 API 端点。
- 真实上游模型 ID。
- 适用产品：Chat、Agent、外部 API。
- 入站协议与上游协议。
- 能力覆盖：文本、视觉、生图、文件、搜索、工具、结构化输出和 WebSocket。
- 优先级、权重、成本、售价策略、地域和代理策略。
- 健康状态、最近探测时间、冷却状态和可选固定路由规则。

这是一种多对一映射：多个来源、多个真实模型都可以共同承载同一个后台平台模型。数据表也允许一个真实上游模型被映射给多个平台模型，以支持不同套餐、价格或能力组合。

用户选择平台模型后，路由器不会直接按名称查上游，而是先按本次产品和请求能力筛选映射目标，再执行额度、健康、代理、粘滞、优先级和故障转移判断。同一会话选定目标后继续遵守会话粘滞，不能因为多个目标指向同一平台模型就随意跳账号。

### 13.3 能力字段

- 文本输入/输出。
- 图像输入。
- 图像生成/编辑。
- 文件上传/引用。
- 网页搜索。
- 函数工具、并行工具、服务端工具。
- JSON Schema/结构化输出。
- 推理强度和推理摘要。
- 上下文长度、最大输出 Token。
- SSE、WebSocket、批处理。
- 提示缓存和缓存计费。

平台模型声明某项能力时，至少需要一条当前可用的映射目标真实支持该能力。一次请求只进入满足本次全部能力的目标；例如带图片生成工具的请求不得被分配给只有文本能力的目标。

### 13.4 各产品模型列表

- Chat 前端读取 Chat 产品发布列表。
- Agent 读取 Agent 产品发布列表。
- 外部兼容 API 的 `GET /v1/models` 读取该 API Key Scope 对应的平台模型列表。
- OAuth/第三方 API 同步得到的原始模型只进入管理后台的上游模型目录，不直接成为任何用户前端选项。

一个平台模型对某个产品可选择时，必须同时满足：

- Key/用户 Scope 允许。
- 产品或项目允许。
- 钱包和套餐允许。
- 至少一个健康上游可提供。
- 调用入口协议有原生或已测试适配器。

管理员可以保留暂不可用的平台模型作为配置草稿，但用户端必须禁用并明确显示“暂不可用”，或者按发布策略隐藏；不能允许点击后再无理由失败。管理后台提供“同步上游模型”“验证能力”和“发布平台模型”三个不同操作，并显示各自时间与结果。

## 14. 协议矩阵

| 入站协议 | 同协议上游 | 异协议上游 | 规则 |
|---|---|---|---|
| OpenAI Responses | Responses | Anthropic/Gemini | 优先原生；跨协议按能力矩阵转换 |
| OpenAI Chat Completions | Chat | Responses/Anthropic/Gemini | 保留兼容字段；工具和多模态需专项测试 |
| Anthropic Messages | Messages | Responses/Gemini | 显式映射 content block、tool_use、stop_reason |
| Gemini GenerateContent | Gemini | Responses/Anthropic | 映射 parts、functionCall、safety 与流事件 |

转换规则：

- 同协议尽可能透传 JSON 和流事件，仅修改认证、目标模型、租户命名空间字段和被禁止的头。
- 异协议先转为内部规范化 IR，再生成目标协议；原始请求和规范化摘要分别审计，但敏感正文按保留策略处理。
- 不认识的字段默认不跨协议猜测含义。
- 工具调用 ID 在转换时维护双向映射，且映射必须属于当前租户和会话。
- SSE 必须保持事件顺序、结束事件和错误语义。
- 客户端断开后立即取消上游 Context，停止计费等待和资源占用。
- WebSocket 只对能力矩阵明确支持的路由开放，不用 SSE 假装 WebSocket。

## 15. 工具、生图、文件和搜索

### 15.1 工具调用

- Agent 工具定义由正式客户端随版本发布并由服务端策略筛选，用户不能任意替换平台工具定义。
- 网页 Chat 不调用本地 Agent 工具。网页搜索、生图和文件理解属于 Chat 的服务端/模型能力，不获得本机项目权限。
- 文件读取、目录查看、只读搜索等低风险 Agent 工具可由用户按项目授权，并在项目授权有效期内复用。
- Shell、文件修改、文件删除、浏览器控制、系统设置、凭证读取和其他高风险 Agent 工具每次执行都必须由本机用户明确确认，不能用一次确认永久放行。
- 每次工具调用记录会话、模型、工具名、参数摘要、批准者、设备、耗时、状态和费用。
- 工具输出进行大小限制、内容类型校验和提示注入边界标记。
- 网页退登、管理员撤销设备、用户停用或设备密钥失效后，项目授权、进行中的工具调用和本地子 Key 立即失效。

### 15.2 生图

- 生图是独立能力，不以模型名称中包含 `image` 作为唯一判断。
- 支持原生图片生成接口、Responses 内图片工具和已验证的兼容接口。
- 记录尺寸、质量、张数、输入/输出资源、供应商请求 ID、费用和内容安全结果。
- 图片文件采用随机对象 ID，不把上游临时 URL 直接作为永久链接。
- 1 核 2G 默认将大文件落盘或对象存储，禁止完整图片长期驻留内存。

### 15.3 文件

- 上传采用流式写盘、大小限制、MIME 嗅探、扩展名校验和随机文件名。
- 文件按用户/项目隔离，不允许路径穿越和跨租户对象 ID 猜测。
- 上游文件 ID 绑定到连接器账号和会话，故障转移时不得误用另一个账号的文件 ID。
- 定时清理过期临时文件，删除事件进入审计。

### 15.4 网页搜索

- 仅在模型和上游真实支持时开放。
- 记录是否启用搜索及搜索调用次数，不记录不必要的敏感查询正文。
- 对 URL 抓取工具执行与第三方 Base URL 相同的 SSRF 防护。

## 16. 账号池、路由与隔离

### 16.1 Key 不再默认固定一个账号

API Key 采用共享账号池的成熟分组路由方式：Key 默认绑定权限组/路由池，而不是固定绑定单个上游账号。路由池根据产品、平台模型、协议和供应商包含多个 OAuth 账号或第三方 API 端点，使一个 Key 能够安全使用池内总可用额度。管理员仍可为诊断或专属客户创建固定上游策略，但这不是默认模式。

核心规则：

- Key 只决定用户/角色、产品 Scope、允许的平台模型、Token 额度和可使用的路由组，不向用户暴露账号池成员。
- 同一个路由组可以包含多个上游账号和第三方端点；不同组的数据、额度、粘滞关系和错误状态完全隔离。
- 首次请求从健康候选中按重置时间、剩余额度、优先级、权重、延迟和冷却状态选择目标。
- 同一 `Key + 用户 + 产品 + 项目 + 会话 + 协议` 建立不可逆粘滞，后续请求优先回到原目标，避免对话、文件和工具上下文串台。
- 账号耗尽、停用、重新授权、进入冷却或被删除后立即退出候选池；仅在请求尚未产生不可重放副作用时安全切换目标。
- 路由选择、粘滞命中、失败切换和最终账号全部进入调用记录，但普通用户只能看到平台模型与自己的用量。
- 账号层不设置产品人为并发上限，但保留上游限流识别、服务器背压和安全连接上限。

推荐粘滞键：

```text
HMAC(platform_namespace_key,
     tenant_id || user_id || api_key_id || project_id || conversation_id || protocol)
```

该值只用于内部路由，不能反推出原始身份。

### 16.2 调度顺序

候选上游必须依次通过：

1. 供应商和模型能力匹配。
2. 账号启用、Token 有效或可刷新。
3. 代理健康。
4. 当前额度未耗尽。
5. 不在冷却或熔断。
6. 项目、套餐、地域和内容策略允许。
7. 当前会话资源可在该账号使用。

候选排序推荐：

- 有有效粘滞关系时优先粘滞账号。
- 无粘滞时优先即将发生额度重置且仍有余额的账号，减少跨周期浪费。
- 其次按健康分、延迟、剩余额度和最近使用时间加权。
- 不能用完全随机选择。

### 16.3 故障转移

- 连接前失败、限流且无副作用时可换账号。
- 已开始输出、已创建文件、已提交工具结果或上游可能产生计费副作用时不自动重放。
- 会话依赖上游文件、对话 ID、缓存 ID 时，账号不可无损切换；应返回可解释错误或由规范化上下文创建新会话。
- 故障转移必须记录原账号、目标账号、原因和重试次数。
- 每个请求全局最多有限次数尝试，避免账号池风暴。

### 16.4 防串台

- 所有缓存、单飞锁、会话映射和工具调用映射的 Key 都必须包含租户范围。
- 禁止把上游 conversation ID 原样暴露给另一个用户。
- API Key、用户 Cookie、桌面会话和本地子 Key 使用不同认证表和哈希前缀。
- 流式响应对象不能跨请求复用缓冲区中的未清理数据。
- 日志正文默认关闭；开启调试也必须脱敏 Token、Cookie、Authorization、文件和个人信息。

## 17. 钱包、充值与计费

### 17.1 规则

- 用户不能自带上游密钥。
- 使用能力前必须拥有平台赠送额度、购买额度或管理员充值。
- Chat 与 Agent 分别建立钱包账户、套餐额度桶和用量统计，禁止合并计算或自动相互抵扣。
- 网页 Chat 发起的请求只扣 Chat 额度；桌面端、CLI、插件和其他 Agent 入口发起的请求只扣 Agent 额度。Agent 内使用普通 Chat 能力仍属于 Agent 请求，因此扣 Agent 额度。
- Chat 使用网页搜索、生图、文件理解等服务端/模型能力时仍属于 Chat 请求，因此扣 Chat 额度；网页 Chat 不调用本地 Agent 工具。
- 外部 API Key 使用独立的 Key 额度/账本，不自动消耗用户的 Chat 或 Agent 套餐。
- 每个钱包账户余额都不是可随意覆盖的数值，而是指定 `product_scope` 的不可变账本聚合结果。
- 充值、赠送、套餐和管理员调整必须明确目标范围为 `chat`、`agent` 或 `external_api`，不能创建含义不明的混合额度。
- 所有人工调整必须填写原因并记录操作者、前后余额和审计事件。

### 17.2 官方套餐基准与平台初始套餐

截至 2026-08-02，OpenAI 官方公开资料显示：Free 为 $0/月、Go 为 $8/月、Plus 为 $20/月；Pro 5x 为 $100/月，Pro 20x 为 $200/月。官方对 Codex/Agent 公布的是按模型和任务复杂度变化的五小时消息范围，例如 Plus 的本地消息约为 Sol 10–100、Terra 25–200、Luna 250–2,000，Pro 5x/20x 大体按倍数提高，并可能另外存在周限制。官方同时明确指出，上下文、推理、工具、检索和缓存都会改变实际消耗，因此官方没有提供可直接等价为固定月 Token 的套餐额度。

官方依据：

- [OpenAI ChatGPT Work 与 Codex 套餐价格及使用限制](https://learn.chatgpt.com/docs/pricing)
- [OpenAI API Token 定价](https://developers.openai.com/api/docs/pricing)

因此 Infinite AI 只参考官方价格档位与倍率，不将下面的 Token 数字宣称为 OpenAI 官方额度。平台首次部署写入以下**可编辑初始值**：

| 套餐 | 参考月价 | Chat 月 Token | Agent 月 Token | Chat/Agent 各自滚动 5 小时 Token | 说明 |
|---|---:|---:|---:|---:|---|
| Free | $0 | 100,000 | 50,000 | 10,000 / 5,000 | 体验档，默认不结转 |
| Go | $8 | 2,000,000 | 1,000,000 | 100,000 / 50,000 | 约为 Plus 价格的 40% |
| Plus | $20 | 5,000,000 | 5,000,000 | 250,000 / 250,000 | 平台基准 1x 档 |
| Pro | $100 | 25,000,000 | 25,000,000 | 1,250,000 / 1,250,000 | 默认采用官方 Pro 5x 档位 |

如果管理员希望采用官方 Pro 20x 档位，可保持套餐代码仍为 `pro`，将参考价格调整为 $200，并将 Chat/Agent 月额度各调整为 100,000,000、滚动五小时额度各调整为 5,000,000，无需新增第五种套餐。

所有套餐字段都由管理后台调控，代码中只保留首次初始化种子值：

- 套餐显示名称、说明、排序、状态和币种。
- 月价、计费周期、是否自动续费和是否允许额度结转。
- Chat 月 Token、Agent 月 Token，以及两者各自的滚动五小时 Token。
- Chat/Agent 分别允许的平台模型、项目数量和功能权限。
- 生效时间、下架时间、新用户可购买状态和现有用户续费状态。
- Free 自动分配策略、Go/Plus/Pro 的购买与管理员赠送规则。

套餐每次修改创建不可变版本。用户购买或续费时保存当时的价格、Chat Token、Agent Token、模型权限和有效期快照；管理员改价不能追溯修改已经付款的当前周期，除非管理员执行带原因和审计记录的专项补发/扣减。

### 17.3 Token 计量规则

- 所有文本调用记录真实 `input_tokens`、`cached_input_tokens`、`output_tokens`、`reasoning_tokens` 和 `total_tokens`。
- 上游返回可信 usage 时以真实 usage 为准；未返回时使用与模型匹配的本地 Tokenizer 估算并标记 `estimated=true`。
- 套餐基础扣减单位为 Token，不再使用“请求次数”作为主要额度。
- 默认计费 Token 为输入与输出等真实 Token 之和；缓存、推理或供应商特殊 Token 由平台模型的计费规则明确处理。
- 高成本模型可以配置 Token 倍率，例如 1 个真实 Token 扣除多个“计费 Token”；倍率必须在模型和套餐页面公开显示，调用记录同时保存真实 Token 与实际扣除 Token。
- 生图、音频、搜索和按次收费工具无法准确表示为文本 Token 时，由后台为该能力配置明确的 Token 等价值，并在使用记录中同时展示原始计量单位。
- 额度检查同时验证月 Token 桶和滚动五小时 Token 桶；任一不足都拒绝新请求，但已经开始且产生上游消耗的请求仍按真实用量结算。
- Chat 请求只写入 Chat Token 桶，Agent 请求只写入 Agent Token 桶；不允许后台任务错误地从另一产品余额兜底。

### 17.4 账本类型

| 类型 | 说明 |
|---|---|
| recharge | 管理员或支付充值 |
| grant | 邀请、活动或套餐赠送 |
| reserve | 请求开始前预冻结 |
| settle | 请求完成后按真实用量结算 |
| release | 释放未使用冻结金额 |
| refund | 明确退款 |
| adjustment | 管理员人工调整，必须有原因 |
| expire | 有效期额度到期 |

账本行不可更新或删除；纠错用反向流水。余额变更与使用记录在同一数据库事务中完成。

每条账本流水必须包含 `product_scope`。同一个用户可以同时持有 Chat 和 Agent 套餐，但两边的剩余额度、过期时间、冻结金额和累计消耗分别计算、分别展示。

### 17.5 定价快照

每次请求保存：

- 平台模型、产品发布配置和真实上游模型。
- 价格版本。
- 输入、缓存输入、输出、推理 Token。
- 图片张数、尺寸、质量。
- 工具、搜索、文件或音频计费单位。
- 上游成本、平台售价、折扣、最终扣费。

价格修改只影响新请求，历史账单必须可复算。

### 17.6 请求结算

1. 估算最大可接受费用并冻结。
2. 上游返回 usage 时优先采用真实 usage。
3. 流式响应未返回 usage 时使用经模型校准的 Token 计数器，并标记 `estimated`。
4. 完成后结算并释放差额。
5. 上游明确未执行的失败请求释放全部冻结。
6. 上游已执行但客户端断开的请求仍按真实用量结算。
7. 生图和工具副作用按供应商真实计费结果结算。

### 17.7 支付系统

支付功能从现有本地产品完整迁入统一 Go 后端，包括商品/套餐、订单创建、支付跳转或收款码、异步回调、主动查单、超时关单、退款、对账、订单列表和用户购买记录，但**系统默认关闭支付**。

支付入口的启用条件必须全部满足：

1. 管理员显式开启支付总开关。
2. 至少配置一个真实商户通道及其商户号、签名/验签密钥、回调域名和商品映射。
3. 后端完成配置校验和通道健康测试。
4. 公网回调 URL 使用 HTTPS，且不指向环回、临时域名或未受信任代理。
5. 管理员确认测试订单、异步回调、主动查单和退款/关闭策略已通过。

未满足条件时，Chat 网页端、Agent、用户中心均不显示充值购买入口，只显示管理员赠送或现有余额；不能出现点击后报“未配置”的空页面。

支付安全规则：

- 商户密钥只在管理端提交，AES-GCM 加密保存，普通用户和前端构建产物不可读取。
- 回调必须按通道官方规则验签，验证商户号、订单号、金额、币种和支付状态。
- `provider + merchant_id + provider_trade_no` 和平台订单号均有唯一约束，重复回调幂等返回。
- 前端同步跳转只用于展示，不能直接充值；余额只由已验签异步回调或主动查单确认后入账。
- 创建订单时保存商品、价格和赠送额度快照，后台改价不能改变已创建订单。
- 订单支付、钱包入账和账本流水在同一数据库事务中提交。
- 退款生成反向账本流水并记录原订单，不修改历史充值流水。
- 回调原文按最小必要原则脱敏留存，所有状态变化进入审计。
- 支付总开关关闭后禁止新建订单，但已支付回调仍需安全幂等处理，避免用户已经付款却无法入账。

## 18. API Key、设备凭证与本地子 Key

### 18.1 平台 API Key

- 明文仅在生成时展示；数据库保存 Argon2id/HMAC 鉴权摘要和 AES-GCM 密文以供管理员受控复制。
- Key 具有用户/角色、Scope、路由池、允许模型、额度、到期时间、IP/设备策略和状态。
- 停用或删除后先更新数据库并使内存缓存版本失效，再返回成功；进行中的请求按策略立即取消。
- 所有节点使用短 TTL 缓存或版本推送，不能出现停用后仍可调用的窗口。
- 复制、停用、恢复、删除和更换绑定均进入审计。
- 外部鉴权 API 默认启用，但没有创建有效 Key、Key 已停用、Scope 不符、额度不足或网络/设备策略不通过时一律不能调用。
- 管理员可在后台切换三种模式：`authenticated_public`（默认，允许有效外发 Key）、`official_client_only`（只允许通过官方 Infinite AI 设备签名链路）、`disabled`（全部外部 API 关闭）。模式变更立即撤销不再符合策略的连接和缓存。

### 18.2 IP 策略

- 同时采集可观测的公网 IPv4 与 IPv6；邀请探测可分别访问 IPv4-only 和 IPv6-only 域名。
- 获取不到某一协议时显示“当前网络未观测到”，不能伪造。
- 反向代理环境只信任配置的代理网段所写入的 Forwarded/X-Forwarded-For。
- 动态 IP 用户优先使用设备凭证；IP 白名单可作为附加限制或风险信号。
- 网页 Chat 只使用用户会话、IPv4/IPv6 和风险策略，不绑定 MAC；Agent 额外强制完成设备公钥与 MAC 登记。

### 18.3 设备凭证

- 海报中可以包含一次性导入载荷，但不得把设备私钥放入图片。
- “设备码”应是服务端生成的可撤销绑定凭证或导入令牌，导入后换取设备密钥注册。
- 海报隐藏载荷使用带版本的签名数据，不依赖脆弱的图片像素隐写作为唯一鉴权方式。
- 上传海报时只解析本产品签名载荷，限制图片尺寸和解码资源，防止图片炸弹。

### 18.4 本地子 Key

- 只由已登录且在线的官方桌面端签发。
- 本地代理默认仅监听 `127.0.0.1` 和 `::1`，不得默认监听局域网。
- 子 Key 只存本机安全存储，绑定桌面会话、设备和父用户额度。
- 可设置模型 Scope、项目、有效期和单次/总额度。
- 网页撤销设备、用户退出或桌面端关闭后，子 Key 立即失效。
- 请求仍由桌面端安全转发至平台网关，不能暴露上游凭证。

## 19. 邀请体系

### 19.1 管理端

- “新建邀请”为按钮并打开弹窗。
- 管理员填写角色备注、额度、有效期、绑定模式、允许产品和可选路由策略。
- 创建后只显示角色和邀请链接；识别码单独复制，避免 URL 泄露即可领取。
- 待使用邀请可撤销；已使用、已撤销、已过期邀请可删除历史展示，但审计流水不可删除。
- 撤销后邀请页立即不可访问。

### 19.2 用户领取

1. 打开一次性邀请链接。
2. 页面显示角色名称，输入识别码。
3. 服务端分别探测并展示 IPv4/IPv6。
4. 选择 IP、设备凭证或组合绑定，并填写设备备注。
5. 点击生成 Key。
6. 服务端一次性消费邀请，生成 Key/设备导入载荷和签名海报。
7. 浏览器自动下载海报；提供“没有下载？重试”按钮，重试只允许在 60 秒展示窗口内下载同一份内容，不生成新 Key。
8. 60 秒后清除页面敏感 DOM、撤销领取会话并跳转到 `https://www.bing.com/`。
9. 此后原邀请 URL 返回 404 或 410，不再展示页面。

消费邀请、创建 Key、创建绑定与扣减/赠送额度必须在一个事务中完成。

## 20. 配置指南页

- 独立端口和独立静态资源目录。
- 只有输入有效 Key，或上传含有效签名载荷的海报后才能查看。
- 根据鉴权结果自动填充 Base URL、Key、可选设备码和实际可用模型。
- 模型通过下拉选项改变，默认选择后台设置的默认模型；不能写死不存在的版本。
- 生成的 `config.toml`、`auth.json` 和环境变量片段必须可直接复制使用，并标明哪些字段必填。
- 如果接口认证使用环境变量，`env_key` 表示读取哪个环境变量；没有设备码时不生成设备码字段，但 API Key 的环境变量仍然是必需的。
- 覆盖 CLI、编辑器插件、桌面软件的官方安装与配置步骤。
- 使用 Markdown 文档风格，代码块一键复制。
- `robots.txt` 禁止抓取，并发送 `X-Robots-Tag: noindex, nofollow, noarchive`；这只是降低索引风险，真正保护依靠鉴权。
- 24 小时错误超过 5 次触发临时封禁；32 小时错误超过 10 次触发永久封禁，管理员可解封。
- 指南端使用独立 Cookie 名称；页面关闭无法被服务端可靠感知，因此敏感数据不写持久缓存，使用 `Cache-Control: no-store`、短期会话和 `pagehide` 尽力清理。不得清除管理后台 Cookie。

## 21. Chat 网页端迁移

目标网页端以**当前已经在本地部署并由用户确认的这一版**作为唯一 UI 和功能基线。在此版本上连接统一 Go 后端、补齐真实数据和权限，不重新换成另一套页面，也不使用静态外观替代真实功能：

- 迁移真实路由、侧栏、聊天列表、消息流、模型选择、文件和设置页面。
- 统一登录弹窗的视觉风格，但保留所有可点击功能与真实数据连接。
- 未登录左下角和右上角显示登录入口；登录后展示用户菜单、余额与退出。
- 模型下拉从 `/api/models` 获取，不使用前端假数组。
- 所有操作具有加载、成功、空状态和真实错误状态。
- 管理 API 与用户 API 分离，用户 Cookie 不能调用管理员接口。
- 前端构建产物由 Go 后端嵌入或由反向代理静态托管，避免依赖另一套常驻应用服务。
- 每次迁移前对当前本地版本制作路由、页面、组件和交互清单；迁移后逐项截图和功能回归，侧栏、模型、聊天、设置、登录弹窗、API 管理和下载入口均不能遗漏。
- 后续 UI 优化只能在该基线之上做增量修改，并保留可回滚构建产物。

## 22. Infinite AI 管理后台

后台品牌统一为“Infinite AI 管理后台”，菜单规划如下：

### 22.1 仪表盘

- CPU、内存、存储实时指标。
- 今日调用、今日 Token、今日消费、错误次数。
- 用户数、设备数、API Key 数、项目数、健康上游数。
- 模型调用排名、供应商成本排名、错误分布和最近安全事件。
- 使用 SSE 或 5 秒轮询自动刷新；切到后台标签页时降低频率。
- 指标必须来自系统和数据库，不显示随机或占位数据。

### 22.2 用户与余额

- 用户列表、状态、套餐、余额、累计消费。
- 手工充值/扣减弹窗，必须填写原因并二次确认。
- 设备、网页会话、Agent 会话和一键退登。
- 项目和成员关系。
- 注册策略切换：关闭/邀请制/公开，默认邀请制；显示当前生效时间和最近操作人。
- Free、Go、Plus、Pro 套餐管理；价格、Chat Token、Agent Token、滚动五小时额度、模型权限、功能权限和有效期全部可调。
- 用户详情分别显示 Chat 与 Agent 套餐进度条、已用/剩余 Token 和下次重置时间，禁止合并成一个总额度。

### 22.3 上游账号

- 按供应商筛选 OAuth 账号。
- “添加账号”按钮打开对应授权弹窗。
- 账号状态、计划、额度进度条、重置次数、重置时间、代理、模型数、最后错误。
- 同步额度、同步模型、健康测试、重新授权、停用和删除。
- 删除必须真实执行且立即从调度池移除。

### 22.4 第三方 API

- 添加、编辑、测试、停用和删除 API 端点。
- API Key 掩码、受控复制、模型同步、能力测试、价格配置。
- 供应商端点与 OAuth 账号使用统一路由池抽象，但凭证表分离。

### 22.5 代理池

- 代理、协议、地区、出口 IP、延迟、健康、失败和关联账号。
- 批量测试、冷却、禁用与账号固定绑定。

### 22.6 模型与价格

- 上游模型快照、后台平台模型和 Chat/Agent/外部 API 产品发布配置。
- Chat/Agent/外部 API 可用范围。
- 能力矩阵、价格版本、默认模型和实际探测状态。

### 22.7 API Key

- 单独页面管理 Key，不与邀请混在同一页面。
- 停用、恢复、删除、复制、余额/配额、Scope、路由池、IP、设备和最近使用。
- 状态变更立即生效并显示被取消的活跃请求数量。
- 外部 API 模式可切换为“有效 Key 可调用”“仅 Infinite AI 官方客户端”或“全部关闭”，默认有效 Key 可调用。

### 22.8 邀请

- 单独页面、按钮新建、弹窗配置。
- 待使用可撤销；已使用、已撤销和已过期可删除列表项。
- 展示消费时间、生成 Key、绑定方式和审计链接。

### 22.9 调用与账单

- 请求 ID、用户、Key、设备、项目、协议、模型、上游、Token、费用、状态、延迟和时间。
- 支持筛选、分页和导出；大数据导出采用流式 CSV/JSONL。
- 正文默认不保存，诊断采样需脱敏和短期保留。

### 22.10 支付管理

- 支付总开关，初始和新部署默认关闭。
- 商户通道、商户号、回调地址、密钥状态和真实连通性测试。
- 商品/套餐、定价、赠送额度、上下架和购买限制。
- 待支付、已支付、已关闭、退款中、已退款和异常订单。
- 主动查单、对账差异、回调失败和人工处理审计。
- 只有后端判定真实商户配置完整且总开关开启时，用户端才获得 `payment_available=true`。

### 22.11 系统与安全

- 管理员密码、TOTP、会话和恢复码。
- 服务健康百分比及每项真实检查详情。
- Nginx/Caddy 配置文件存在性、权限、哈希变化与监控状态；未安装时显示“未安装/未配置”，不显示成系统故障。
- 404/502 阈值、临时/永久封禁、解封和保护 IP。
- 数据导出、导入、备份校验和主密钥状态。
- 审计日志、使用日志、安全事件和运行错误分栏。
- 数据保留设置展示账单/充值 90 天、使用/审计/安全 180 天、诊断正文最长 24 小时及最近自动清理结果。

## 23. 数据模型

### 23.1 核心实体

| 实体 | 关键字段 |
|---|---|
| users | id、email、display_name、password_hash、status、created_at |
| admins | id、username、password_hash、totp_secret_enc、role、status |
| sessions | type、token_hash、csrf、user/admin、ip、expires_at、revoked_at |
| devices | user_id、public_key、mac_hash/enc、platform、status、last_seen |
| projects | owner_id、name、policy_id、budget、status |
| project_members | project_id、user_id、role |
| providers | kind、name、auth_mode、status、policy_json |
| upstream_accounts | provider_id、identity、credential_enc、status、proxy_policy |
| api_endpoints | provider_id、base_url、credential_enc、protocol、status |
| proxies | url_enc、region、egress_ips、health、cooldown |
| route_pools | provider/model 策略、选择算法、状态 |
| route_pool_members | pool_id、account/endpoint、weight、priority |
| upstream_models | upstream_id、real_model_id、capabilities、pricing、snapshot_version |
| platform_models | public_id、display_name、description、capability_policy、status |
| product_model_publications | platform_model、product_scope、price_version、sort、status |
| model_route_targets | platform_model、upstream/account/pool、real_model_id、capabilities、priority、weight |
| plans | code、name、currency、price、billing_cycle、status、current_version |
| plan_versions | plan、Chat/Agent Token、滚动窗口、模型权限、功能权限、effective_at |
| subscriptions | user、plan_version、price snapshot、starts_at、expires_at、renewal_status |
| api_keys | key_hash、key_enc、owner、scope、pool_id、quota、status |
| affinities | tenant/key/project/session_hash、route_member、expires_at |
| conversations | user、project、product、model、status |
| usage_events | request、token、tool/image 单位、upstream、status、cost |
| wallet_accounts | user_id、product_scope、currency、available、reserved、version |
| quota_buckets | user_id、product_scope、source、total、remaining、expires_at |
| ledger_entries | wallet、product_scope、immutable type、amount、reference、price snapshot |
| payment_providers | kind、merchant_id、credential_enc、callback、enabled、verified_at |
| products | name、price、currency、credit_amount、status、version |
| payment_orders | user、provider、product snapshot、amount、status、trade_no、expires_at |
| payment_events | order、provider event id、signature result、status、payload digest |
| invitations | role、token_hash/enc、code_hash、status、expires_at |
| audit_events | actor、action、target、ip、detail、created_at |
| security_events | ip/device/user、kind、severity、detail、created_at |
| banned_subjects | subject_type、value、scope、reason、expires_at |

### 23.2 数据完整性

- 金额使用整数最小单位或定点十进制，禁止 float。
- 所有业务时间统一存 UTC Unix 毫秒，前端按用户时区显示。
- 软删除实体保留 `deleted_at`；密钥删除同时擦除密文并保留不可逆审计标识。
- 每个外键定义删除行为，不允许孤儿账单和用量记录。
- 钱包使用乐观版本或单事务串行更新。
- 调用记录通过唯一请求 ID 幂等写入。
- OAuth 身份在 `provider_id + upstream_subject` 上唯一。
- PostgreSQL 关键表按租户/用户外键和必要的行级约束设计；后台查询全部显式带权限条件，不单独依赖前端筛选。
- 账本、订单、邀请消费和 Key 状态变更使用 PostgreSQL 事务、唯一约束和行锁保证并发正确性。

### 23.3 当前数据迁移重点

- 当前 `api_keys.account_id` 迁移为可选固定账号，并新增 `route_pool_id`。
- 当前账号表拆分公共账号字段与供应商特有凭证元数据。
- 现有 ChatGPT 模型快照迁移为通用模型快照。
- 现有使用记录补充用户、设备、项目、供应商、协议、成本和计费状态字段。
- 现有桌面用户迁移为统一 users，不丢失 GPT 上游账号。
- 当前 SQLite 通过只读一致性快照导出，转换并导入 PostgreSQL 暂存 Schema；比较表行数、关键哈希、外键和业务汇总后才切换。
- 迁移前生成加密备份，迁移失败自动回滚事务和二进制版本。

## 24. 全链路鉴权与安全

### 24.1 请求入口顺序

```text
连接限制 -> 可信代理解析 -> IP 封禁 -> 路由级限速 -> Body 上限
-> 内容类型校验 -> 身份鉴权 -> CSRF/签名/Nonce -> RBAC/Scope
-> 项目/模型/工具策略 -> 钱包预授权 -> 上游调用 -> 结算/审计
```

### 24.2 常见防护

1. 管理员/用户密码强哈希与 TOTP。
2. 管理端和用户端 Cookie 隔离、Secure、HttpOnly、SameSite。
3. 所有状态变更接口 CSRF 与 Origin 校验。
4. API Key 常量时间比较、哈希存储、密文受控复制。
5. 桌面请求设备签名、时间窗、Nonce 和 Body Digest。
6. 每 IP、用户、Key、设备、路由的分层令牌桶限速。
7. 404、401、403、429、502 异常计数和临时/永久封禁。
8. 参数化 SQL、严格 JSON 解码、字段长度和枚举校验。
9. SSRF、DNS Rebinding、重定向和私网地址防护。
10. 文件路径穿越、符号链接逃逸、压缩炸弹和图片炸弹防护。
11. 安全响应头、CSP、禁止 iframe、MIME 嗅探保护。
12. 上游响应体和 SSE 单事件大小限制。
13. 错误信息分级，外部不返回数据库、路径、Token 或堆栈。
14. 主密钥文件 0600、数据目录 0700；PostgreSQL 使用独立最小权限账号、SCRAM-SHA-256 和本机/私网访问控制。
15. 敏感操作重新认证、双确认和不可变审计。
16. 依赖锁定、SBOM、漏洞扫描和构建产物校验和。
17. OAuth State/PKCE/Nonce 一次性校验和短期过期。
18. 登录、邀请、指南、Token 刷新和导入接口独立限速。
19. 备份加密、格式签名、大小限制和导入事务隔离。
20. 停用用户/Key/设备/账号时撤销缓存并取消活跃请求。

### 24.3 注入防护

- SQL 只用占位符，排序字段从服务器白名单选择。
- HTML 默认转义；Markdown 禁止原始 HTML 或使用严格净化器。
- Shell 工具使用参数数组和项目沙箱，禁止服务端将用户字符串拼入命令。
- 日志使用结构化字段，替换换行和控制字符，防止日志注入。
- 模型输出始终被视为不可信内容；不能直接决定后台权限或工具批准。

### 24.4 即时撤销闭环

- 数据库状态是权威，缓存条目带实体版本。
- 停用事务提交后广播本进程失效事件并取消关联 Context。
- API Key 中间件在每个新请求确认缓存版本；高风险路由直接读数据库。
- 流式长连接定期检查撤销 Channel；撤销后发送通用错误并关闭。
- 验收要求：停用接口返回成功后的第一个新请求必须拒绝，已有流在规定时间内中止。

## 25. 可观测性与真实数据

### 25.1 每次调用记录

- request_id、trace_id。
- 调用来源：Chat、Agent、CLI、插件、外部 API、本地子 Key。
- 用户、API Key、设备、项目、会话的内部 ID。
- 入站协议、路径、平台模型、产品范围、真实上游模型、连接器、账号/端点和代理。
- 输入、输出、缓存、推理和总 Token。
- 图片、工具、搜索、文件的计量单位。
- 上游成本、平台售价、冻结、结算和退款。
- HTTP/上游状态、标准错误码、耗时、首 Token 延迟、重试和故障转移。
- 客户端 IP 的 IPv4/IPv6 观测结果和可信代理链摘要。

### 25.2 实时仪表盘

- 后端每 5 秒计算轻量快照并缓存，避免每个浏览器重复扫描全表。
- 活跃页面用 SSE 推送或 5 秒轮询；断线指数退避。
- 今日统计用按小时聚合表，原始日志异步归档。
- CPU 从 `/proc/stat` 两次采样计算，内存从 `/proc/meminfo`，存储从数据目录所在文件系统读取。
- 不支持的平台明确标记“不可用”，不返回 0 冒充真实值。

### 25.3 健康百分比

健康百分比由带权检查项计算，并显示分母和每项状态：数据库、磁盘、主密钥、上游、代理、模型同步、配额同步、日志写入、备份、反向代理监控。未配置项从分母排除并显示原因。

### 25.4 数据保留与自动清理

- 账单明细、充值流水、支付订单和退款明细默认在线保留 3 个月（90 天）。
- 使用记录、管理员审计和安全事件默认保留 180 天。
- Chat 对话不按固定时间强制删除：用户可主动删除，管理员可配置全局或套餐级保留期。
- 默认不保存原始 API 请求正文、响应正文、Authorization、Cookie、OAuth Token、设备私钥或完整工具输出。
- 管理员临时开启诊断采样时，正文必须先脱敏、限制采样范围并设置明确过期时间，最长保留 24 小时，到期自动物理删除。
- 每日维护任务按小批次删除到期数据，避免大事务锁表；每次清理保存类型、时间范围、删除行数和任务结果，不保存被删除正文。
- 账单/充值明细到期删除前生成带完整性校验的月度账户期初、变动汇总和期末余额快照，保证三个月后钱包仍可校验，但不继续保留逐笔明细。
- 如果部署地区的财税、退款或争议处理规则要求更长时间，管理员可以把保留期调长；不能配置短于上述默认值而破坏当前产品约定。

## 26. 数据导出、导入和灾备

### 26.1 导出包

导出为版本化加密归档：

```text
manifest.json          # 版本、时间、表清单、校验和
database.dump          # PostgreSQL 自定义格式一致性备份
objects/               # 必要附件或图片
config.public.json     # 非敏感部署设置
integrity.sig          # 完整性签名
```

- 导出前使用 PostgreSQL 一致性快照与自定义格式备份，不能复制正在运行的数据目录。
- 归档使用管理员提供的导出密码通过 Argon2id 派生密钥，并使用 AEAD 加密。
- 明确选择是否携带上游凭证；携带时必须二次认证和醒目标记。
- 下载使用流式响应，临时文件权限 0600，完成后清理。

### 26.2 导入

- 只接受支持的格式版本、签名和校验和。
- 解压限制文件数、单文件大小、总大小和路径。
- 在隔离的临时 PostgreSQL 数据库或 Schema 中验证备份、迁移、约束、序列和业务汇总。
- 停止新请求、排空活动请求，在受控维护窗口切换数据库连接目标并刷新所有缓存。
- 失败自动切回原数据库和服务；成功后原数据库进入只读保留期，不立即销毁。
- 新环境必须重新确认公网 URL、TLS、可信代理和 OAuth 回调 URL。

### 26.3 备份策略

- 每日自动备份，保留 7 日；每周保留 4 份，可配置。
- 备份成功不等于可恢复，每月至少自动执行一次隔离恢复校验。
- 主密钥与加密备份不能只保存在同一台服务器。

## 27. 1 核 2G 性能方案

### 27.1 默认资源预算

| 组件 | 目标常驻内存 |
|---|---:|
| Go 网关与后台 | 120–250 MB |
| PostgreSQL | 180–400 MB（小规格参数） |
| 静态网页 | 由同进程提供，无额外常驻服务 |
| 反向代理 | 20–60 MB |
| 系统余量 | 至少 1 GB |

### 27.2 优化规则

- PostgreSQL 小规格默认建议从 `shared_buffers=128MB`、`effective_cache_size=512MB`、`work_mem=4MB`、`maintenance_work_mem=64MB`、`max_connections=30` 起步，再按实测调整。
- Go 使用受限连接池，初始建议最大连接 15、空闲连接 5，并设置连接寿命和查询超时。
- 调用日志批量入库；高频汇总使用 PostgreSQL 增量聚合，不在仪表盘请求中全表扫描。
- 默认不再常驻 Redis、第二套数据库、消息队列和对象存储。
- SSE 流式转发使用固定小缓冲，不完整缓存响应。
- 上传和下载流式处理；限制 Body、单 SSE 事件和并发大文件。
- 模型、额度、健康同步加抖动并限制并发，避免整点风暴。
- 仪表盘读聚合表，日志表建立按时间和租户的正确索引。
- 定期按时间分区/归档历史调用记录，监控 PostgreSQL WAL、Autovacuum、慢查询和磁盘增长。
- Go 设置合理 `GOMEMLIMIT`，systemd 设置文件句柄、内存和重启策略。

### 27.3 性能验收目标

- 10 个以内活跃用户、20 个长连接时服务稳定。
- 网关自身非上游处理 P95 小于 50 ms（本机测试，不含数据库冷启动和大文件）。
- 100 个并发空闲 SSE 连接不导致内存线性失控。
- 24 小时稳定性测试无持续内存增长、数据库锁死或日志丢失。

“不限制账号并发”表示产品层不设置人为账号并发上限，但仍保留服务器安全连接上限、上游限流感知和背压，防止 1 核 2G 被请求洪峰拖死。

## 28. 部署与发布

### 28.1 Bash 一键部署

空白 Ubuntu 20.04/22.04 服务器脚本负责：

1. 检测架构、系统版本、磁盘和端口。
2. 安装最小运行依赖、CA 证书和受支持的 PostgreSQL 版本。
3. 下载带 SHA-256 和签名的发布包。
4. 创建不可登录系统用户、PostgreSQL 最小权限角色和 `/var/lib/infinite-ai` 0700 数据目录。
5. 生成数据库随机密码、主密钥和强管理员初始凭证，凭证只显示一次。
6. 安装 systemd 服务、日志轮转和反向代理模板。
7. 启动服务并执行健康检查。
8. 输出各端口和首次初始化方式。

脚本必须幂等，升级前自动备份，失败回滚二进制。不得从源码临时执行不受版本控制的网络脚本链。

### 28.2 GitHub 一键部署

- Actions 构建 Linux amd64/arm64 二进制、桌面安装包和前端资源。
- 发布 Release、校验和、SBOM、签名和变更说明。
- Docker 镜像为可选交付，使用非 root 用户、只读根文件系统和固定版本基础镜像。
- 工作流只使用仓库 Secrets，不把生产配置写入构建产物。
- Release 前必须通过单元、集成、安全、迁移、痕迹和许可证扫描门禁。

### 28.3 Linux 桌面包

- Ubuntu 20.04 与 22.04 分别在干净容器/虚拟机验证 `sudo dpkg -i`。
- 包声明真实依赖，不能要求用户手工安装开发环境。
- 图标、Desktop Entry、MIME、卸载和升级路径均验收。
- 不携带旧产品用户数据；卸载数据清理由用户明确选择或按已授权迁移策略执行。

## 29. 测试矩阵

### 29.1 单元测试

- 各连接器 Token 解析、刷新、错误映射和能力转换。
- OAuth State/PKCE/Nonce、回调 URL 验证和一次性消费。
- 路由候选、粘滞、额度重置优先和熔断。
- 分组账号池隔离、会话粘滞、账号耗尽迁移和不可重放请求保护。
- 钱包冻结、结算、释放、退款和并发幂等。
- 套餐版本、Chat/Agent 独立 Token 桶、滚动五小时窗口、Token 倍率和估算标记。
- API Key、设备签名、Nonce、CSRF、RBAC 和 Scope。
- IP 解析、可信代理、IPv4/IPv6、封禁阈值。
- 协议 IR 的工具、生图、流事件和停止原因转换。

### 29.2 集成测试

- 每种 OAuth 的成功、拒绝、过期、错误 State、刷新和重新授权。
- 每种第三方兼容协议的模型、普通响应、SSE、工具、图片、文件和错误。
- Chat 登录、聊天、模型选择、余额不足和退出。
- 三种注册策略的界面与接口行为，以及邀请并发消费只能成功一次。
- 支付关闭时全端无入口；启用真实测试商户后的创建订单、验签回调、重复回调、主动查单、关单、退款和对账。
- Agent 浏览器登录、设备绑定、项目工具、网页退登即时生效。
- Agent 缺少 MAC 时拒绝注册、稳定 MAC 改变时重新验证、网页 Chat 不请求 MAC。
- 用户可创建本地项目并选择允许模型，但直接写入供应商、Base URL、上游 Key、代理、系统提示词或平台工具时返回 403。
- 外部 API 默认只接受有效 Key；切换官方客户端专用或全部关闭后现有连接立即失效。
- Key/邀请停用和删除后的第一请求立即失败。
- 代理成功、认证失败、出口变化、超时和熔断。
- 数据导出到干净环境导入后继续使用。

### 29.3 安全测试

- SQL/XSS/Markdown/日志/命令注入。
- CSRF、CORS、Origin 绕过、Cookie 混淆。
- SSRF、DNS Rebinding、重定向到内网、IPv6 编码绕过。
- 路径穿越、符号链接、Zip Slip、图片/压缩炸弹。
- OAuth 回调重放、Token 刷新竞态、设备 Nonce 重放。
- 越权读取其他用户项目、聊天、文件、Key、账单和调用记录。
- 404/502/登录/邀请/指南爆破封禁与管理员解封。
- 账单/充值 90 天、用量/审计/安全 180 天和调试正文 24 小时的边界清理测试；清理后钱包汇总仍一致。

### 29.4 上游真实验收

每种已配置连接器至少用一个受控测试账号验证：

- 授权和自动刷新。
- 模型列表与实际可调用一致。
- 文本和流式响应。
- 工具调用往返。
- 视觉输入、生图、文件、搜索等其声明能力。
- 额度同步和重置时间。
- 代理绑定。
- 错误、限流、额度耗尽和恢复。

没有真实凭证或上游未开放的项目只能标记“代码路径已测试/待真实上游验收”，不能写“完全可用”。

## 30. 验收标准

### 30.1 功能验收

- 管理后台品牌和菜单统一，无旧品牌可见文案。
- Chat 与 Agent 使用同一用户身份，但钱包账户、套餐额度和用量统计完全分开。
- 前端只显示管理员发布的平台模型，不直接显示 OAuth 或第三方 API 的原始模型列表。
- 同一个平台模型可同时由 OAuth 账号池和第三方 API 的多个真实模型承载，并能按能力正确筛选目标。
- 用户端不存在供应商、Base URL、上游 Key 和系统提示词编辑入口；直接调用接口也返回 403。
- 管理员可添加、测试、停用和删除真实上游。
- 用户可选择的平台模型至少有一个满足能力的健康映射；无健康映射的配置草稿只能隐藏或明确禁用。
- 外部 API、Chat、Agent 和本地子 Key 全部形成用量与费用记录。
- 邀请撤销/消费、Key 停用/删除、用户退登立即生效。
- 数据可导出并在新环境恢复。
- 注册默认邀请制；切换关闭/公开后前后端行为立即一致。
- 支付默认关闭且用户端无入口；只有真实商户配置校验通过并开启后才显示。
- Free/Go/Plus/Pro 初始种子正确，后台改价改额生成新版本且不追溯改变已购买周期。
- 同一用户的 Chat 与 Agent 月 Token、滚动五小时 Token 分别扣减；任一产品耗尽不能消耗另一产品额度。
- Agent 必须通过设备公钥签名并登记物理网卡 MAC；MAC 改变要求重新验证，网页 Chat 不绑定 MAC。
- 外部 API 默认仅凭有效 Key 调用，并可即时切换为官方客户端专用或全部关闭。
- 自动清理严格执行已确认保留期，且不保存原始敏感正文。

### 30.2 隔离验收

- 两个用户并发进行 100 轮多工具对话，无 conversation、tool_call、file、日志或响应串台。
- 同一用户两个项目的工具权限和文件 ID 不互通。
- 故障转移只发生在安全阶段，并能完整追踪。
- 日志和导出不包含明文 Token、密码、Cookie 或设备私钥。

### 30.3 运行验收

- Ubuntu 20.04/22.04 全新机一键安装成功。
- 1 核 2G 目标负载稳定 24 小时。
- 服务重启不丢账单、不重复消费邀请、不复活已撤销会话。
- PostgreSQL 不可用、连接池耗尽、磁盘满、日志写失败和上游全故障均在后台真实显示。

## 31. 分阶段实施顺序

### 阶段 0：冻结边界与备份

- 确认本文中的关键决策。
- 建立现有功能清单和真实回归基线。
- 备份数据库、上游账号和管理员配置。
- 建立敏感信息与外部痕迹扫描门禁。
- 固化当前本地网页版本的构建产物、截图、路由和功能清单，作为后续唯一前端基线。

完成标准：现有 ChatGPT 账号、Key、邀请、设备和用量均可恢复。

### 阶段 1：PostgreSQL、品牌、单一用户体系和前端恢复

- 建立 PostgreSQL Schema、迁移命令、连接池、健康检查和备份恢复。
- 将当前 SQLite 数据一次性迁移到 PostgreSQL，核对后切换，不长期双写。
- 后台统一更名。
- 以当前本地已部署网页版本为唯一基线接入真实 Go API。
- 统一用户、Cookie、网页会话和桌面设备。
- 实现关闭/邀请制/公开注册策略，默认邀请制。
- 恢复所有侧栏、模型选择和原有交互，清除假按钮/假数据。

完成标准：登录、Chat、管理端和桌面登录回归通过。

### 阶段 2：通用上游模型

- 抽象连接器、供应商、账号、API 端点、代理和模型快照。
- 迁移现有 ChatGPT 数据。
- 实现第三方 OpenAI/Anthropic/Gemini 兼容 API。
- 建立能力矩阵和协议 IR。
- 建立后台平台模型、产品发布配置和 `model_route_target`，支持多个 OAuth/第三方真实模型共同指向一个平台模型。
- Chat、Agent 和外部 API 改为读取各自的平台模型发布列表，不直接读取原始上游模型。

完成标准：现有能力无回归，三种兼容协议通过流式和工具测试。

### 阶段 3：多 OAuth 与代理池

- 按供应商逐个实现授权、刷新、模型、额度和健康。
- 实现每账号代理和代理池。
- 每完成一个连接器就独立真实验收，不等待全部完成后一起排错。

建议顺序：ChatGPT 基线 → Claude → Gemini → Kiro → xAI → Antigravity。最终是否启用取决于官方授权条件和真实测试结果。

### 阶段 4：路由池与会话隔离

- Key 从固定账号迁移到路由池。
- 实现粘滞、重置优先、熔断和安全故障转移。
- 完成跨用户、跨项目、跨工具压力隔离测试。

完成标准：一个 Key 可安全使用池内多个账号额度，同时同一会话不无故跳账号。

### 阶段 5：钱包与计费

- 建立 Chat、Agent、外部 API 独立钱包账户和额度桶，以及带 `product_scope` 的不可变账本、价格版本、冻结和结算。
- 创建 Free/Go/Plus/Pro 种子套餐、后台全字段调控、套餐版本快照和滚动五小时 Token 桶。
- 管理员手工充值、消费明细和余额不足处理。
- Token、生图、文件、工具和搜索计费。
- 完整迁入支付商品、订单、回调、查单、关单、退款和对账；默认关闭。
- 通过真实测试商户验收后，才允许管理员开启用户端支付入口。

完成标准：并发请求不超扣、不少扣，历史账单可复算。

### 阶段 6：Agent 项目化与本地子 Key

- 删除用户上游自定义入口及服务端写接口。
- 允许用户创建本地项目和选择后台允许模型，服务端锁死供应商、Base URL、上游 Key、代理、系统提示词和平台工具配置。
- 项目成员、模型、低风险只读授权、高风险逐次确认和预算策略。
- 浏览器登录、设备公钥签名、物理网卡 MAC 登记、MAC 变化重新验证和即时退登。
- 本机回环代理和受限子 Key。

完成标准：用户无法通过 UI 或直接 API 写入供应商配置；Agent 无有效设备公钥/MAC 绑定时不能使用；设备撤销立即生效；网页 Chat 不要求 MAC。

### 阶段 7：安全、备份、部署和发布

- 完成安全门禁、反向代理监控、自动封禁。
- 导出、导入、恢复演练。
- 一键 Bash、systemd、Docker/GitHub Release。
- Ubuntu 20.04/22.04 安装包验收和 24 小时稳定性测试。

## 32. 关键冲突与推荐决定

| 冲突 | 推荐决定 | 原因 |
|---|---|---|
| Key 固定账号 vs 使用账号池总额度 | 默认绑定路由池，保留可选固定账号 | 兼顾额度利用和特殊隔离需求 |
| Agent 可配置供应商 vs 平台集中控制 | 用户禁止配置，管理员集中配置 | 满足计费、审计和安全要求 |
| MAC 绑定 vs 动态 IP | 设备公钥为主，IP/MAC 为风险信号 | MAC 无法通过公网 HTTP 可靠获取且可伪造 |
| “全部透明透传” vs 跨协议 | 同协议透明；跨协议显式适配 | 私有字段和流事件无法天然无损 |
| “无限制速率” vs 1 核 2G 和防攻击 | 不设产品人为并发，保留安全上限和背压 | 防止资源耗尽，保护正常用户 |
| 多套后台融合 | Go 后端唯一权威，旧后端仅迁移数据/功能 | 避免用户、余额和权限冲突 |
| SQLite 现状 vs PostgreSQL 目标 | 一次性校验迁移到 PostgreSQL，不长期双写 | 保证一个权威数据库和复杂账本事务能力 |
| 网页端重做 vs 保留当前本地版 | 当前本地部署版本是唯一功能/UI 基线 | 避免再次丢失原有交互和真实功能 |
| 注册开放程度 | 三态策略，默认邀请制 | 兼顾封闭运营、灰度邀请和未来公开注册 |
| 支付完整功能 vs 暂不开放 | 完整迁入，默认关闭，真实商户验收后显示 | 避免假入口和未配置支付风险 |
| Chat 与 Agent 是否共用套餐 | 钱包账户、额度桶和用量完全分账 | 防止一个产品消耗另一个产品的套餐权益 |
| 前端模型名 vs 真实上游模型 | 前端只展示后台平台模型 | 产品命名、套餐和上游路由解耦 |
| OAuth 与第三方 API 同时承载一个模型 | 多条映射共同指向同一平台模型 | 支持统一产品入口、负载与故障转移 |
| OAuth 必须全部可用 vs 官方开放条件 | 完成适配框架，只有官方流程和真实验收后启用 | 避免不稳定、违规或伪功能 |
| 管理员可复制 Key vs 只存哈希 | 哈希鉴权 + AES-GCM 密文受控复制 | 满足复制需求并降低数据库泄露风险 |
| 海报隐藏信息 vs 安全设备凭证 | 海报携带签名导入令牌，不携带设备私钥 | 图片泄露时仍可撤销、过期和审计 |
| 网页 Chat 与本地 Agent 工具 | 网页 Chat 不调用本地工具；本地工具只属于已绑定设备的 Agent | 浏览器不能静默获得本机执行权限 |

## 33. 洁净室实现与痕迹门禁

本项目只吸收公开产品中可观察到的通用架构思想和协议事实，所有实现独立设计、独立编码和独立测试。

当前工作区是长期迭代的代码基线，历史目录中仍可能存在旧品牌、旧来源命名或先前导入内容。因此“本次新增文档和品牌改动未引入外部痕迹”不等于“整个历史代码库已经完成洁净室验收”。正式融合实施必须先盘点现有命中项：能证明为本项目原创的内容执行独立改名；来自外部且不能证明独立实现的内容不能只删除署名或许可证，必须由本项目规格和测试驱动重新实现后再替换。未完成这一门禁前不得发布宣称无外部痕迹的源码包。

执行规则：

1. 外部源码只允许存在于工作区外的临时只读目录。
2. 不复制源码、测试、文案、图标、样式、数据库迁移、脚本或配置模板。
3. 不保留外部项目名称、仓库地址、目录名、包名、注释、作者信息或提交历史。
4. OAuth、HTTP 和模型协议以官方文档和真实请求测试为实现依据。
5. 每个模块先写本项目接口、数据模型和验收测试，再独立实现。
6. 提交前扫描文本、二进制字符串、Git 历史、依赖清单、许可证和资源元数据。
7. 发现相似命名或资源时停止发布，确认来源并重写或替换。
8. 第三方依赖必须是明确选择的通用开源库，保留其依法要求的许可证，不得通过“去痕迹”规避依赖许可证义务。
9. 对当前仓库做一次全量来源清单，区分“通用依赖”“本项目原创”“待洁净重写”三类；待洁净重写代码不得进入正式发布分支。
10. 清理后重新生成全仓文本命中清单、源码包文件清单和二进制字符串清单，并由人工复核零命中例外。

CI 门禁至少包含：

- 禁止名称/URL 字符串扫描。
- Secret 扫描。
- 许可证与 SBOM 扫描。
- 图片哈希和元数据检查。
- Git 子模块、嵌套 `.git` 和意外 vendoring 检查。
- 生成产物与源码包内容清单检查。

## 34. 已确认决策与上线前外部条件

以下内容已经由用户确认，不再作为待选项：

1. 后端为一个总的 Go 后端。
2. 目标权威数据库为 PostgreSQL。
3. Chat 网页端使用当前已经在本地部署的这一版作为唯一基线。
4. 用户注册支持关闭、邀请制、公开，默认邀请制。
5. 支付能力完整迁入但默认关闭，只有配置并验证真实商户后才在用户端显示。
6. Chat 与 Agent 使用同一用户身份，但套餐额度、钱包账户和用量统计完全分账。
7. 用户前端只展示管理员在后台创建并发布的平台模型，不直接展示 OAuth 或第三方 API 的真实模型列表。
8. 多个 OAuth 账号池和第三方 API 的真实模型可以同时映射到同一个后台平台模型。
9. API Key 默认绑定分组路由池，按会话粘滞使用池内账号；特殊 Key 才由管理员固定上游。
10. 套餐按 Token 计量，分为 Free、Go、Plus、Pro；价格、Chat/Agent Token、滚动窗口、模型和功能权限全部由后台版本化调控。
11. 用户可创建本地项目并选择后台允许的模型，但不能设置供应商、Base URL、上游 Key、代理、系统提示词或平台工具。
12. 网页端只有 Chat，不调用本地 Agent 工具；Agent 同时支持 Chat 和工具。
13. Agent 的低风险只读工具可按项目授权；Shell、修改、删除、浏览器控制等高风险工具每次确认，网页退登或设备撤销立即失效。
14. 使用 Agent 必须绑定设备公钥和物理网卡 MAC；设备公钥为硬身份，MAC 与 IPv4/IPv6 为强制登记的辅助信号，MAC 改变触发重新验证而非永久封禁。网页 Chat 不绑定 MAC。
15. 外部鉴权 API 默认开启但必须有有效 Key；管理员可切换为仅 Infinite AI 官方客户端或全部关闭。
16. 账单和充值流水保留 3 个月；使用、审计、安全记录保留 180 天；Chat 对话由用户删除或管理员配置；原始请求/响应正文默认不保存，脱敏调试正文最长 24 小时。
17. 单 Go 后端和 PostgreSQL 为最终默认部署形态，达到明确容量阈值后才拆分非核心异步能力。

核心产品架构已无待确认项。以下属于上线前必须取得或验证的外部条件，不通过时相应能力保持“未配置/不可用”：

1. Claude、Gemini、Antigravity、xAI、Kiro 的官方授权条件、客户端凭证、Scope、回调地址和真实测试账号。
2. 各第三方 API 的真实 Base URL、平台密钥、模型、价格和能力验证结果。
3. 支付商户号、签名/验签密钥、HTTPS 回调域名、测试订单和退款/对账验证。
4. 实际 OAuth 账号池容量、额度重置规律和模型成本，用于管理员在上线前调整套餐种子额度，避免超卖。

实施严格按“阶段 0 → 阶段 7”推进。每个阶段独立提交、独立回归、可回滚；未通过验收的能力不得在后台显示为可用。
